import { Dimensions, StatusBar } from "react-native";

export const { width, height } = Dimensions.get("window");
export const STATUSBAR_HEIGHT = StatusBar.currentHeight;
export const HORIZONTAL = 20; //padding | margin
